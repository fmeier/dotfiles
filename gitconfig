[alias]
	b = branch
	ci = commit
	st = status
	co = checkout
	lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
[core]
	excludesfile = /Users/fmeier/.gitignore
[push]
	default = simple
[user]
	name = fmeier
	email = franzi.meier@gmail.com
[credential]
	helper = store
[filter "lfs"]
	required = true
	clean = git-lfs clean %f
	smudge = git-lfs smudge %f
